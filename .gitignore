# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

#init files
*.ini

#ENV files
.env

Agents.MD
.vscode
docs/

#user-specific folders
scripts/docker/
alembic.ini

# Local SQLite database (dev/test artifact)
file_converter.db
*.db
*.sqlite3

# ---------------------------------------------------------------------------
# Local-only AI agent tooling. These live on the developer's machine and must
# never be committed or pushed. They are intentionally untracked.
# ---------------------------------------------------------------------------
.agents/
.claude/
.github/agents/
.github/skills/
.github/hooks/
.github/prompts/
.impeccable/
skills-lock.json

# Production secrets backup. Never commit this; keep real values in the
# deployment secret store / environment, not in the repository.
.env.production.backup
