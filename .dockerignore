# Git
.git
.gitignore
.gitattributes

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/

# Environment & secrets
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Local data
logs/
*.log

# Local databases (dev/test artifacts)
*.db
*.sqlite3

# Local-only AI agent tooling — never ship these inside the image
.agents/
.claude/
.impeccable/
skills-lock.json
.github/agents/
.github/skills/
.github/hooks/
.github/prompts/
Agents.MD

# Production secrets backup — must never be baked into an image
.env.production.backup

# Frontend dependencies and build output. node_modules is large and is
# reinstalled by `npm ci` in the frontend build stage; a locally-built dist
# must never leak in, because that stage builds its own.
web/node_modules/
web/dist/

# Deployment helpers not needed inside the image
deployment/docker/compose.yaml
deployment/docker/nginx.conf
